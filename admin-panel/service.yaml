apiVersion: v1
kind: Service
metadata:
  labels:
    app: pos-admin-panel
  name: pos-admin-panel
  namespace: pos-admin-panel
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: pos-admin-panel
  type: NodePort
status:
  loadBalancer: {}
